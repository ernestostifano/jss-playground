import {createUseStyles} from 'react-jss';

const useStyles = createUseStyles({
    root: {
        height: 100,
        backgroundColor: '#fe00fe',
        color: '#ffffff',
        fontSize: 20
    }
});

export {useStyles};
